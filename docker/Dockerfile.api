FROM node:18-alpine
WORKDIR /app

# Copy from server directory (relative to root context)
COPY server/package.json server/package-lock.json ./
RUN npm install --production

# Copy all server files
COPY server/ ./

EXPOSE 5000
CMD ["node", "index.js"]